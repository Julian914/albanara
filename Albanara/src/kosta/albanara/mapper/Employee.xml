<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kosta.albanara.mapper.EmployeeMapper">
	<cache />
	<insert id="insertEmployee" parameterType="Employees">
		insert into employees(
			employee_seq, employee_id, employee_pw, employee_name, employee_birthday,
			employee_email, employee_gender, employee_phone, employee_address) 
			values(s_employee_seq.nextval, #{employeeId}, #{employeePw}, #{employeeName}, #{employeeBirthday}, #{employeeEmail}, 
			#{employeeGender}, #{employeePhone}, #{employeeAddress})
	</insert>
	
	<!-- 이력서 등록 (동현)-->
	<insert id="insertResume" parameterType="Resumes">
		insert into resumes value(resume_seq.nextval, #{picture_filename}, #{resume_title}, #{resume_contents}, #{desired_working_area}, #{favorite_field}, sysdate)
	</insert>
	
	<!-- 이력서 조회  (동현)-->
	<select id="searchResume" parameterType="int" resultType="Resumes">
		select * from resume where seq=#{employeeSeq}
	</select>
	
	<update id="updateEmployee" parameterType="Employees">
		update employees set employee_pw = #{employeePw}, employee_email = #{employeeEmail}, 
			employee_address = #{employeeAddress}, employee_phone = #{employeePhone} where employee_id = #{employeeId}
	</update> 

	
	
</mapper>